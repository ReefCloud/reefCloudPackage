% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_geoserver_data.R
\name{get_geoserver_data}
\alias{get_geoserver_data}
\title{Get Data from Geoserver}
\usage{
get_geoserver_data(
  Tier = as.numeric(BY_TIER) - 1,
  cov_name = NULL,
  rc_client,
  use_cache = TRUE,
  timeout_seconds = GEOSERVER_DEFAULT_TIMEOUT,
  initial_chunk_size = GEOSERVER_DEFAULT_CHUNK_SIZE
)
}
\arguments{
\item{Tier}{An integer indicating the tier of the data to be retrieved.}

\item{cov_name}{A string indicating the name of the coverage to be retrieved.}

\item{rc_client}{WFS client object from get_geoserver_info()}

\item{timeout_seconds}{Timeout in seconds for each request (default: GEOSERVER_DEFAULT_TIMEOUT)}

\item{initial_chunk_size}{Initial chunk size for pagination (default: GEOSERVER_DEFAULT_CHUNK_SIZE)}
}
\value{
A spatial data frame containing the filtered data from the geoserver.
}
\description{
Downloads and filters spatial data from the geoserver for a specified tier and coverage name.
Includes adaptive chunking, retry logic, timeout handling, and progress indication.
}
\details{
Requires BY_TIER, DATA_PATH, and rc_client to be available in the environment.
Implements adaptive chunk sizing, exponential backoff retry, and progress indication.
}
\examples{
get_geoserver_info()
cov_data <- get_geoserver_data(Tier = 4, cov_name = "degrees_heating_weeks_tier", rc_client)
}
\author{
Murray Logan
}
