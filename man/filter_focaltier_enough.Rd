% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_focaltier_enough.R
\name{filter_focaltier_enough}
\alias{filter_focaltier_enough}
\title{Filter Focal Tiers with Sufficient Spatio-Temporal Coverage}
\usage{
filter_focaltier_enough(data.grp, FOCAL_TIER, n.spat, n.temp, i, N)
}
\arguments{
\item{data.grp}{A data frame of survey records to filter.}

\item{FOCAL_TIER}{A character string naming the column identifying focal tiers.}

\item{n.spat}{Integer. Minimum number of distinct spatial sites (lat/lon combinations) per tier.}

\item{n.temp}{Integer. Minimum number of distinct years per tier.}
}
\value{
A list:
\describe{
  \item{data.grp.enough}{Data with tiers meeting the spatio-temporal thresholds.}
}
}
\description{
Filters `data.grp` based on spatial and temporal thresholds defined by `n.spat` (minimum spatial sites)
and `n.temp` (minimum temporal observations) for each level of `FOCAL_TIER`. 
Only tiers with enough data coverage are retained.
}
\examples{
\dontrun{
result <- filter_focaltier_enough(data.grp, FOCAL_TIER = "Tier5", n.spat = 10, n.temp = 3)
result$filtered_data
}
}
\author{
Julie Vercelloni
}
